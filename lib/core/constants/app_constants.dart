class AppConstants {
  // Flag to control visibility of commercial data (prices, earnings)
  // Set to false during Google Play review if required
  static bool data = true; // "data" flag as requested
}
